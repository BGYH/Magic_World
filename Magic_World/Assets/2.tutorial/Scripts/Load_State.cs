using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using System.Text;

public class Load_State : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Place_three_wand place = GameObject.Find("GameObject").GetComponent<Place_three_wand>();

        //string fileName = "TestJson";
        //string path = Application.dataPath + "/" + fileName + ".Json";

        //FileStream filestream = new FileStream(path, FileMode.Open);
        //byte[] data = new byte[filestream.Length];
        //filestream.Read(data, 0, data.Length);
        //filestream.Close();
        //string json = Encoding.UTF8.GetString(data);

        //PlayerState myplayerState = JsonUtility.FromJson<PlayerState>(json);

        //Debug.Log(myplayerState.wand1);
        //Debug.Log(myplayerState.wand2);
        //Debug.Log(myplayerState.wand3);

        //place.wand1 = myplayerState.wand1;
        //place.wand2 = myplayerState.wand2;
        //place.wand3 = myplayerState.wand3;

        //place.turn = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
